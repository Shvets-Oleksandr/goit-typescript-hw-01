let unionType: string | number | boolean;

unionType = "word";
unionType = 20;
unionType = false;

type EnableOrDisable = "enable" | "disable";
let status: EnableOrDisable;

status = "enable";
status = "disable";